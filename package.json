{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend server",
  "main": "server.js",
  {
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "postbuild": "node scripts/prepare-deploy.mjs",
    "start": "next start",
    "lint": "eslint ."
  }
}
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
